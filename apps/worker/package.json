{
  "name": "@signal-hub/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/telemetry-worker.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/main.ts",
    "test": "vitest run --root ../.. apps/worker/test/telemetry-worker.test.ts"
  },
  "dependencies": {
    "@signal-hub/config": "workspace:*",
    "@signal-hub/db": "workspace:*",
    "@signal-hub/queues": "workspace:*",
    "@signal-hub/telemetry": "workspace:*",
    "bullmq": "^5.76.4",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
