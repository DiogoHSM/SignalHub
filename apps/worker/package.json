{
  "name": "@sigmon/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/telemetry-worker.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/main.ts",
    "test": "vitest run --root ../.. apps/worker/test/telemetry-worker.test.ts apps/worker/test/backups.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1044.0",
    "@sigmon/config": "workspace:*",
    "@sigmon/db": "workspace:*",
    "@sigmon/queues": "workspace:*",
    "@sigmon/telemetry": "workspace:*",
    "bullmq": "^5.76.4",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
