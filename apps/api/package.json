{
  "name": "@signal-hub/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^9.1.3",
    "@signal-hub/config": "workspace:*",
    "@signal-hub/db": "workspace:*",
    "@signal-hub/queues": "workspace:*",
    "@signal-hub/telemetry": "workspace:*",
    "fastify": "^5.8.5",
    "ioredis": "^5.10.1",
    "kysely": "^0.28.16",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@signal-hub/worker": "workspace:*",
    "vitest": "^2.1.8"
  }
}
