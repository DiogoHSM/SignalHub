{
  "name": "@sigmon/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "sigmon-mcp": "./dist/stdio.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.2"
  }
}
