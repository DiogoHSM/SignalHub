{
  "name": "@signal-hub/queues",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.76.4",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "testcontainers": "^11.14.0",
    "vitest": "^2.1.8"
  }
}
