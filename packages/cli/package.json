{
  "name": "@sigmon/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "sigmon": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
